package com.kuzhi.findme.client;

final class FindMeReleaseFeatures {
    // Keep the unfinished companion ability implementation intact without exposing it in release builds.
    static final boolean COMPANION_ABILITIES = false;

    private FindMeReleaseFeatures() {
    }
}
